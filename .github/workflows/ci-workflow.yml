name: Continuous Integration
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-20.04
    strategy:
      fail-fast: false
      matrix:
        ruby: ['2.7', '3.0', '3.1', '3.2']
        gemfile: ['gemfiles/low_spec.gemfile', 'Gemfile']
        exclude:
          # Old Capybara versions aren't designed to work with Ruby 3+
          - { ruby: '3.0', gemfile: 'gemfiles/low_spec.gemfile' }
          - { ruby: '3.1', gemfile: 'gemfiles/low_spec.gemfile' }
          - { ruby: '3.2', gemfile: 'gemfiles/low_spec.gemfile' }
    env:
      BUNDLE_GEMFILE: ${{ matrix.gemfile }}
    steps:
      - uses: actions/checkout@v3
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler: '2.2.33'
          bundler-cache: true
      - run: bundle exec rspec
      - run: bundle exec rubocop
